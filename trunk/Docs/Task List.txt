1. ScriptEdit underlines verification errors and output window displays verification/compilation errors, meaning compiler
   errors are never underlined.

2. CommandNode relies on a hardcoded script folder to resolve script-calls

3. Cleanup new vArg flags in parameter/command syntax

4. script-calls and argument name resolution are both case sensitive, make them case insensitive

# Properly format script editor tooltips

# [DONE] Fix matching of current suggestion in auto-complete

# Connect saving of script documents to output file

# [DONE] Highlighting not updated after Indent/Outdent 

# [DONE] Toggle comment is broken - the text becomes mangled if it's indented

# Typing in the script edit doesn't modify the document









